{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "DATABASE_URL=file:./dev.db npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "if [ -d \"/app/data\" ]; then export DATABASE_URL=\"file:/app/data/dev.db\"; fi && npx prisma db push && npm run start"
  }
}
